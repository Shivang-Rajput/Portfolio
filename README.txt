Put your photo here as "profile.jpg" (square-ish photos look best,
the frame crops to a 1:1 ratio). The About component reads it from
"/profile.jpg" — see src/data.js.

Also drop your resume PDF here as "resume.pdf". The Resume button in
the nav opens it in a modal (src/data.js -> resumeUrl) with a working
download button.
